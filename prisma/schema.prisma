generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model B2BOrder {
  id        Int      @id @default(autoincrement())
  name      String
  email     String
  product   String
  quantity  String
  createdAt DateTime @default(now())
}
